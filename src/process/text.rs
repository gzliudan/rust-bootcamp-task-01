pub fn encrypt_text(key: &str, msg: &str) {
    println!("encrypt text: key={}, msg={}", key, msg);
}

pub fn decrypt_text(key: &str, msg: &str) {
    println!("decrypt text: key={}, msg={}", key, msg);
}

pub fn generate_text_key() {
    println!("generate_text_key");
}
